<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title></title>
        <link rel="stylesheet" href="../css/styles.css" media="screen" title="no title" charset="utf-8">
    </head>
    <body>
        <ul>
            {% for review in user_reviews%}
            <li><a href="/restaurant/{{review.getRestaurant_id}}">{{(restaurants[0].getById(review.getRestaurant_id)).getName}}</a>:
            {{review.getReview}}</li>
            {% endfor %}
        </ul>
        <a href="/">Go home</a>
    </body>
</html>
